# ---------------------------------------------------------------------------
# Núcleo — suficiente para o modo demo, os testes e o benchmark em QUALQUER
# máquina (Windows, Linux, macOS, Raspberry Pi):
# ---------------------------------------------------------------------------
numpy>=1.26
flask>=3.0
psutil>=5.9

# ---------------------------------------------------------------------------
# Hardware/visão — apenas no dispositivo alvo (Raspberry Pi ou máquina com
# webcam). Podem falhar em Python > 3.12; veja o README.
# ---------------------------------------------------------------------------
opencv-python>=4.8      # captura de vídeo (no Raspberry Pi OS prefira: sudo apt install python3-opencv)
mediapipe>=0.10.9       # extração de landmarks (requer Python 3.9 a 3.12)
pyttsx3>=2.90           # síntese de voz (alternativa leve: sudo apt install espeak-ng)
